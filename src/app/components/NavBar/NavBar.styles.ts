import styled from 'styled-components';


export const NavWrapper = styled.div`
  align-items: center;
  background: linear-gradient(90deg, rgba(0, 0, 0, 0.89) 0%, rgba(14, 13, 13, 0.77) 100%);
  display: flex;
  justify-content: space-between;
  padding: 2rem;
  margin-top: 2rem;
  position: absolute;
  width: 100%;
  box-shadow: 0 4px 8px rgba(70, 69, 69, 0.93);
`

export const LogoWrapper = styled.div`
  display: flex;
  align-items: flex-end;
  justify-content: flex-end;
`
export const LogoTitle = styled.span`
  color: #fff;
  font-size: 2.6rem;
  font-weight: 600;
  margin-left: 0.8rem;
  line-height: 1;
`

